export type LoginUser = {
    email: string;
    password: string;
};

export type SignupUser = {
    name: string;
    secondName: string;
    email: string;
    phoneNumber: string;
    password: string;
    passwordRepeat: string;
};
