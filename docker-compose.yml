services:
    app:
        build:
            context: .
            dockerfile: server/Dockerfile
        ports:
            - "3000:3000"
        env_file:
            - server/.env.production.local
